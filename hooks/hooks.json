{
  "description": "hormozi-gtm: SessionStart banner + AI-isms check after Write/Edit in outputs/.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'hormozi-gtm plugin active. Alex Hormozi persona stays on for every /hormozi-gtm:* command. Humanizer is required on external outputs (LPs, scripts, hooks, email). Outputs save to outputs/<type>/. First run: use /hormozi-gtm:init to create gtm-context.md.'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/check-aiisms.py\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
